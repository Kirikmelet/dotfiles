;;c-ftconf.el
;; For C Language

(setq c-default-style
      (quote(
             (c-mode . "linux")
             (java-mode . "java")
             (awk-mode . "awk")
             (other . "linux")
             )
            )
      )

(provide 'c-ftconf)

